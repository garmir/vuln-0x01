name: Unique Test - vuln-0x01
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - vuln-0x01
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: vuln-0x01 ===" > unique-test-vuln-0x01.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-vuln-0x01.txt
            echo "Test ID: TEST_006" >> unique-test-vuln-0x01.txt
            echo "Timestamp: $(date)" >> unique-test-vuln-0x01.txt
            curl -s ifconfig.me >> unique-test-vuln-0x01.txt
            echo "Unique instruction: Vulnerability analysis and security testing" >> unique-test-vuln-0x01.txt
          '

      - name: Ubuntu Fallback - vuln-0x01
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for vuln-0x01" > fallback-vuln-0x01.txt
          curl -s ifconfig.me >> fallback-vuln-0x01.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-vuln-0x01
          path: "*.txt"
          retention-days: 2